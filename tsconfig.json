{
  "extends": "@react-native/typescript-config/tsconfig.json",
  "compilerOptions": {
    "baseUrl": "./apps/",
    "paths": {
      "assets": ["assets"],
      "commons": ["commons"],
      "cores": ["cores"],
      "screens": ["screens"],
      "storages": ["storages"],
    }
  }
}
